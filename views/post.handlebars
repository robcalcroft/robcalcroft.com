{{#if error}}
  <b>{{{ error }}}</b>
{{else}}
  <article class="post">
    <h1>{{ title }}</h1>
    <div class="date">{{ created }} / <a href="/">Go Home</a></div>
    <section>{{{ content }}}</section>
  </article>
{{/if}}
